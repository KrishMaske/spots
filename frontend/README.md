# Frontend

Current status: empty placeholder.

No web framework has been selected and no web client is scaffolded. Do not add
or document Next.js, Vite, React, SvelteKit, or frontend CI unless a web client
is explicitly being created.

If a web client is added later, it should consume the same public backend
OpenAPI contract as the mobile client.
